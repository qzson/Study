import numpy as np # import는 땡겨오겠다. numpy는 수학적인 api, as는 numpy를 np를 부르겠다.
x = np.array([1,2,3]) # 1,2,3 이라는 숫자그룹을 x로 지정하겠다.
y = np.array([1,2,3])

# 일차함수 y = ax + b 일차함수 즉, 회귀, 선형회귀(기울기 선으로 된)

'''
1. 노드의 갯수
2. 레이어의 깊이 Deep
3. epoch
4. batch_size
이것들을 수정하는 것이 하이퍼 파라미터 튜닝

자바의 카멜케이스?
SnowWhitePrincess.java
그러나, c언어나 파이썬 사용자들은 
snow_white_princess 가 익숙하다.

'''